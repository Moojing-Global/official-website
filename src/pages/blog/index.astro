---
import MainLayout from "@layouts/MainLayout.astro";
import { getCollection } from "astro:content";

// SEO metadata
export const seoMeta = {
    title: "Blog",
    description: "Read our latest blog posts and articles.",
    canonical: "/blog",
};

const posts = await getCollection("blog");
const sortedPosts = posts.sort(
    (a, b) =>
        new Date(b.data.pubDate).getTime() - new Date(a.data.pubDate).getTime(),
);
---

<MainLayout {seoMeta}>
<<<<<<< HEAD
    <section class="px-4 py-16">
        <div class="mx-auto w-full max-w-7xl px-4">
            <div class="mb-12 text-center">
                <h1 class="text-foreground mb-4 text-4xl font-bold md:text-5xl">
                    Blog & Insights
                </h1>
                <p class="text-muted-foreground mx-auto max-w-2xl text-lg">
                    Discover the latest trends, insights, and best practices in
                    enterprise data analytics and digital transformation.
                </p>
            </div>

            <div class="grid gap-8 md:grid-cols-2 lg:grid-cols-3">
                {
                    sortedPosts.map((post) => (
                        <article class="bg-card border-border flex flex-col overflow-hidden rounded-lg border transition-shadow hover:shadow-lg">
                            {post.data.featured_image && (
                                <div class="aspect-video w-full overflow-hidden">
                                    <img
                                        src={post.data.featured_image}
                                        alt={post.data.title}
                                        class="h-full w-full object-cover transition-transform hover:scale-105"
                                    />
                                </div>
                            )}
                            <div class="flex flex-1 flex-col p-6">
                                <div class="mb-3 flex items-center gap-4 text-sm">
                                    <time class="text-muted-foreground">
                                        {post.data.pubDate.toLocaleDateString(
                                            "en-US",
                                            {
                                                year: "numeric",
                                                month: "long",
                                                day: "numeric",
                                            },
                                        )}
                                    </time>
                                    {post.data.author && (
                                        <span class="text-muted-foreground">
                                            by {post.data.author}
                                        </span>
                                    )}
                                </div>
                                <h2 class="text-foreground mb-3 text-xl font-semibold">
                                    <a
                                        href={`/blog/${post.slug}`}
                                        class="hover:text-primary transition-colors"
                                    >
                                        {post.data.title}
                                    </a>
                                </h2>
                                {post.data.description && (
                                    <p class="text-muted-foreground mb-4 flex-1">
                                        {post.data.description}
                                    </p>
                                )}
                                {post.data.tags && post.data.tags.length > 0 && (
                                    <div class="mt-auto flex flex-wrap gap-2">
                                        {post.data.tags.slice(0, 3).map((tag) => (
                                            <span class="bg-muted text-muted-foreground rounded-full px-3 py-1 text-xs">
                                                {tag}
                                            </span>
                                        ))}
                                    </div>
                                )}
                            </div>
                        </article>
                    ))
                }
            </div>
        </div>
=======
    <section class="mx-auto w-full max-w-7xl px-4 py-16">
        <h1>Blog Posts</h1>
        <ul>
            {
                sortedPosts.map((post) => (
                    <li>
                        <a href={`/blog/${post.slug}`}>
                            <h2>{post.data.title}</h2>
                            {post.data.description && (
                                <p>{post.data.description}</p>
                            )}
                            <time>
                                {post.data.pubDate.toLocaleDateString()}
                            </time>
                        </a>
                    </li>
                ))
            }
        </ul>
>>>>>>> 40f8636d2ed6626c29c31b5226e8b047a688726f
    </section>
</MainLayout>
